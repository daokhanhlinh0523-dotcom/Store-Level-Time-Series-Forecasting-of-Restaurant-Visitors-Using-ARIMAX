This folder stores archived non-final experiment outputs that were removed from the main `output/` directory.

The final result set for grading is the `*_main250cv.*` files kept directly under `output/`.
